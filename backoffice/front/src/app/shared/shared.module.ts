import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

// Importez ici les composants, directives, pipes, etc. partagés

@NgModule({
  declarations: [
    // Liste des composants, directives, pipes, etc. partagés
  ],
  imports: [
    CommonModule
  ],
  exports: [
    // Liste des composants, directives, pipes, etc. à exporter
  ]
})
export class SharedModule { }
